import Icon from 'supercons';


const SuperCons = ({ size, glyph }) => {
    return (
        <Icon  glyph={glyph} size={size} style={{cursor:"pointer"}}/>
    )
}

export default SuperCons
